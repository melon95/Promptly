# act 配置文件
# 用于本地测试 GitHub Actions

# 使用的平台映射（因为 act 无法直接使用 macOS runner）
-P macos-14=catthehacker/ubuntu:act-latest
-P macos-latest=catthehacker/ubuntu:act-latest

# 默认使用的 secrets 文件
--secret-file .secrets

# 启用详细输出
--verbose

# 不自动拉取 Docker 镜像
--pull=false 